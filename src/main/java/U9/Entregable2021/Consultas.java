package U9.Entregable2021;

/**
 * PagosCliente() que solicita al usuario un número de cliente (customerNumber)
 * y muestre por pantalla el nombre del cliente (contactFirstName), su apellido (contactLastName),
 * país (country) y la suma de todos los pagos realizados por dicho cliente.
 * Esta consulta será realizada con un Statement.
 *
 * InfoPedido() que solicita al usuario el número del pedido (orderNumber)
 * y muestra por pantalla el nombre, precio y cantidad de todos los productos incluídos
 * en el pedido así como el coste total del mismo. Está consulta será realizada con un PreparedStatement.
 *
 * DatosJefe() que solicita el número de un empleado (employeeNumber)
 * y muestra por pantalla el nombre(firstName) y los apellidos(lastName) de su jefe.
 * Está consulta será realizada con un PreparedStatement.
 */
public class Consultas {


}
